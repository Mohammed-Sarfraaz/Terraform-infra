apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-webapp
  namespace: demo
  labels:
    app: simple-webapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: simple-webapp
  template:
    metadata:
      labels:
        app: simple-webapp
    spec:
      imagePullSecrets:
        - name: acr-secret
      containers:
        - name: webapp
          image: REPLACE_WITH_ACR/simple-webapp:latest
          ports:
            - containerPort: 80
